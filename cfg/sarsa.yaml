env:
  size: [6, 6]
  max_steps: 200
  seed: 0
  layout: "default"

agent:
  algo: sarsa
  alpha: 0.1
  gamma: 0.95
  epsilon: 0.3
  epsilon_decay: 0.95
  epsilon_min: 0.01

train:
  episodes: 1000
  log_every: 200